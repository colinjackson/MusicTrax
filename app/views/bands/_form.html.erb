<form action="<%= form_action %>" method="post">
  <%= authenticity_token %>
  <%= patch_method if form_method == :patch %>
  
  <p><%= resource_name("band", band.name) %></p>
  
  <p><input type="submit" value="<%= submit_text %>"></p>
  
</form>